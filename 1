        # Crie pasta para os splits originais
mkdir -p splits_originais

# Extraia o base.apk (é o split principal)
unzip -o bet365_original.apk -d splits_originais base.apk

# Extraia os splits de configuração (se houver)
unzip -o bet365_original.apk -d splits_originais split_config.*.apk

# Extraia os splits de arquitetura (ARM, x86, etc.)
unzip -o bet365_original.apk -d splits_originais "lib/*"
